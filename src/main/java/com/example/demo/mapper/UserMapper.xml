<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
    <insert id="insertUser" parameterType="User">
        INSERT INTO USERS(USER_NAME, USER_EMAIL, USER_PASSWORD, USER_PHONE, USER_BIRTHDAY, CREATEDAT, UPDATEDAT) VALUES(#{user_name},#{user_email},#{user_password},#{user_phone}, #{user_birthday}, sysdate, sysdate)
    </insert>


    <select id="getUser" resultType="User">
        SELECT * FROM USERs WHERE USER_ID=#(user_id) AND USER_PASSWORD = #{user_password}
    </select>

<!-- 
    <delete id="delete" >
        delete from member where id = #{table_id} 
    </delete>

    // @Insert("INSERT INTO tables(table_title, table_autor, table_text) VALUES(#{table.title},#{table.autor},#{table.text})")
    // int insert(@Param("tables")Table table);

    // @Select("SELECT * FROM TABLES")
    // List<Table> getAll();
    -->
</mapper>